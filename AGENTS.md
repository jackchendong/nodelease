# 项目概览

这是一个 Go 公共库，通过 Redis 为同一服务的多个运行实例分配互不重复的 `worker_id`。调用方提供服务名和 Redis 客户端，库从配置范围内随机申请 ID，使用带 TTL 的租约保持所有权，并支持自动续租、手动续租和主动释放。

仓库模块路径为 `github.com/jackchendong/nodelease`。公开 API 与行为以 `README.md` 和 `README.en.md` 为准；两份文档表达同一契约，修改用户可见行为时必须同步更新。

当前项目仍处于初始化阶段。实现功能前先检查 `go.mod`、现有包、测试和文档，不要因为本文件描述了建议结构就无条件创建目录或抽象。

# Go 技能

- 实现、修改或审查 Go 代码前，必须先阅读 `.agents/skills/golang/SKILL.md`。
- 根据任务继续阅读其中列出的相关子技能，例如 Redis、并发、设计、测试、模块和工具链。
- 只加载当前任务需要的子技能；根技能中的路由表决定何时使用它们。

# 核心契约

- `ServiceName` 隔离不同服务的 ID 空间；相同服务名的实例应使用相同范围和租约配置。
- ID 范围为包含两端的闭区间，且满足 `0 <= min <= max`。
- 分配必须依赖 Redis 原子操作；不能使用“先查询、再写入”的非原子流程判断空闲。
- 每次成功分配必须生成新的、不可预测的租约令牌。续租和释放必须同时校验 key 与令牌，不能只按 `worker_id` 操作。
- 租约过期或所有权发生变化后，旧持有者不得继续续租、释放新持有者的 key，或声称仍拥有该 ID。
- 自动续租无法证明绝对所有权。无法在 TTL 到期前确认续租成功时，必须向调用方报告租约丢失。
- `Release` 应幂等；重复释放不能删除后来持有者的租约。
- 不要让库主动退出进程、关闭调用方传入的 Redis 客户端，或强制记录日志。状态和错误交由调用方处理。

# Redis 规则

- 默认 key 形如 `nodelease:{serviceName}:worker:{workerID}`；Redis Cluster 哈希标签应保持同一服务相关 key 位于同一 slot。
- 对 `Namespace` 和 `ServiceName` 建立明确的字符、长度和花括号处理规则，防止 key 冲突和意外改变 Cluster slot。
- 申请优先使用 `SET key token NX PX ttl` 或具备同等语义的原子脚本。
- 续租使用 Lua 脚本比较 token 后执行 `PEXPIRE`；释放使用 Lua 脚本比较 token 后执行 `DEL`。
- Lua 脚本返回值必须映射为稳定的 Go 语义，区分成功、租约不存在、所有权变化和 Redis 执行失败。
- 不要依赖扫描 Redis key 来确定可用 ID。分配尝试必须有上限，并能在有限范围全部占用时可靠返回 `ErrNoWorkerIDAvailable`。
- 网络超时后的写入结果可能不确定。不要把“客户端收到错误”等同于“Redis 没有执行命令”；设计和测试这类边界。
- 保留调用方 context，并让 Redis 命令、重试和脚本执行都支持取消。

# 公共 Go API

- 导出的名称、方法签名、默认值、哨兵错误及其 `errors.Is` 行为都是兼容性契约。
- 所有导出标识符都要有符合 Go 文档规范的注释；包注释说明租约保证和限制。
- 构造函数校验必需依赖和配置，使成功返回的实例可以立即使用。
- Go 零值有业务含义时，不要用它同时表达“未配置”。默认开启的布尔选项应使用语义清晰的反向字段、指针或 Option。
- 可取消或可能阻塞的方法将 `context.Context` 作为第一个参数；不在结构体内保存 context，也不用 `context.Background()` 绕过调用方取消。
- 只在调用方需要稳定分类判断时暴露哨兵错误；包装底层错误时使用 `%w` 保留原因。
- 接收满足实际命令集合的最小接口。若直接接收 `redis.UniversalClient`，不得关闭或重新配置它。
- 库代码不得依赖调用方使用某个日志框架、全局 Redis 客户端或包级可变配置。

# 并发与生命周期

- 每个自动续租 goroutine 都必须有唯一所有者、退出条件和可等待的完成路径。
- `Acquire`、`Renew`、`Release`、`WorkerID` 和续租循环并发调用时必须无数据竞态，并保持租约状态机一致。
- 不要持锁执行 Redis 网络调用。需要跨网络调用保持的状态，使用版本、令牌或明确的状态转换验证结果仍适用。
- timer 和 ticker 由创建者停止并回收。测试和生产同步都不能依赖任意 `time.Sleep`。
- 租约丢失通知不得使后台 goroutine 因无人接收而永久阻塞；通道的容量、关闭者和只通知一次的语义必须明确。
- `Release` 应先阻止新的自动续租，再安全等待正在执行的续租结束，最后基于令牌释放；明确处理取消和并发重复调用。

# 随机性与边界

- 租约令牌使用 `crypto/rand` 生成，并具有足够熵；不得使用 `math/rand`、时间戳或可预测序列作为所有权凭证。
- worker ID 的随机遍历可使用可测试的随机源，但不能引入偏差、无限重试或在范围较小时漏试可用 ID。
- 在分配前检查范围宽度和整数运算溢出。不要分配与巨大用户范围成正比的无界内存。
- duration 使用 `time.Duration` 并校验 Redis 毫秒精度、最小 TTL、续租间隔和转换溢出。

# 依赖与仓库管理

- 使用 Go Modules，模块路径保持为 `github.com/jackchendong/nodelease`。
- Redis 客户端优先采用文档约定的 `github.com/redis/go-redis/v9`，不要并行引入多个 Redis 客户端。
- 使用 Go 命令维护 `go.mod` 和 `go.sum`，不手工编辑 `go.sum`。
- 新增依赖前确认标准库和现有依赖不能合理完成任务，并评估公共库使用者承担的传递依赖成本。
- 不提交编辑器配置、凭据、本地 Redis 数据、测试缓存、构建产物或机器专用路径。
- 不执行与当前任务无关的依赖大版本升级或全仓库机械重写。

# 测试要求

- 测试可观察行为和租约不变量，不把内部字段或具体 goroutine 调度当作契约。
- 至少覆盖：配置校验、范围端点、单 ID 范围、并发申请、范围耗尽、服务隔离、续租成功、令牌不匹配、过期、幂等释放、context 取消和 Redis 错误。
- 并发测试验证同一服务不会同时返回重复 ID，并使用 `go test -race` 检查共享状态。
- 时间相关测试使用可注入时钟、短但有界的 deadline 或明确事件同步，不使用长时间真实休眠。
- fake 适合验证决策逻辑；Lua、TTL、`SET NX PX` 和 Redis Cluster 相关语义应使用真实 Redis 集成测试验证。测试默认不得依赖开发者已有的 Redis 数据。
- 缺陷修复优先增加能复现问题的回归测试，不得通过删除断言、跳过测试或吞掉错误规避失败。

# 质量检查

修改 Go 代码后，根据改动范围依次执行：

```bash
gofmt -w <changed-go-files>
go test ./...
go test -race ./...
go vet ./...
go build ./...
```

修改模块依赖时额外执行：

```bash
go mod tidy
go mod verify
```

若仓库尚未创建 `go.mod`、真实 Redis 不可用或环境不支持某项检查，必须明确报告未运行及原因，不能声称已经通过。

# 文档与发布

- 用户可见的 API、默认值、错误、Redis key 或租约语义变化必须同步修改中英文 README。
- README 示例必须能够在替换必要连接参数后编译；不要记录尚不存在的版本标签为已经发布。
- 发布使用语义化 Git 标签。`v2` 及以上模块路径必须带 `/vN` 后缀。
- 未经明确要求不要创建标签、GitHub Release 或推送破坏性 API 版本。
